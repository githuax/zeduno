# ZedUno Frontend Environment Variables

# API Configuration
# For development (default if not set)
# VITE_API_URL=http://localhost:5000/api

# For production with domain
# VITE_API_URL=https://yourdomain.com/api

# For production with IP
# VITE_API_URL=http://YOUR_SERVER_IP/api

# App Configuration
VITE_APP_NAME=ZedUno
VITE_APP_VERSION=1.0.0

# Features
VITE_ENABLE_ANALYTICS=true
VITE_ENABLE_PAYMENT_GATEWAYS=true
VITE_DEFAULT_CURRENCY=KES

# Development
VITE_DEBUG=false
VITE_LOG_LEVEL=warn

# -----------------------------
# Backend Environment Variables
# -----------------------------

# Node environment: development | production
NODE_ENV=development

# API Server
PORT=5000

# Auth
# Required in production
JWT_SECRET=change_me_in_prod

# CORS allowlist (comma-separated). Required in production.
# Example (prod): https://app.example.com,https://admin.example.com
# Example (dev): http://localhost:5173,http://localhost:3000
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Allow mock auth tokens (only for development). Must be 'true' to enable.
ALLOW_MOCK_AUTH=false

# Zed Business Integration (required in production)
ZED_API_BASE_URL=
ZED_AUTH_TOKEN=
ZED_EXTERNAL_ORIGIN=
# Optional – header name for API key/bearer; defaults to X-Authorization
ZED_API_KEY_HEADER=X-Authorization
